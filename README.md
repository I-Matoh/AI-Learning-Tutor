# AI-Learning-Tutor
Personalized AI tutor for anything!

## Setup
1. Install dependencies:
   `npm install`
2. Create your env file:
   `Copy-Item .env.example .env`
3. Open `.env` and set your Groq key:
   `VITE_GROQ_API_KEY=...`
4. Start the app:
   `npm run dev`
